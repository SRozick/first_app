class PagesController < ApplicationController
	# router sends to Pages controller, looks for method home

	def home
		render :home 
		# looks for a View called home (view home is inside pages dir)
		
	end
end
